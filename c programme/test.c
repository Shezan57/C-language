#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{

    int n;
    scanf("%d", &n);
  	// Complete the code to print the pattern.
      int size = 2 * n - 1;
      int top = 0;
      int right = size-1;
      
     int a[size][size];
     while (n > 0) {
        for ( int i = 0; i <=end; i++)
        {
            for (int j = 0;  < count; i++)
            {
                /* code */
            }
            
        }
        
     }
     ++start;
     --end;
     --n;
     }
     
     for (int i=0; i <= size; i++) {
     for (int j; j <= size; j++) {
     printf("%d",a[i][j]);
     printf("\n");
     }
     }
      
      
    return 0;
}